"use client";

/**
 * Dark mode removed — there is nothing to toggle. Kept as a no-op so existing
 * dock/sidepanel layouts don't need to change shape; renders nothing.
 */
export function ThemeToggle() {
  return null;
}
